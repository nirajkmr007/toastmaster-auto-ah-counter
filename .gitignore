# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Vosk model tarballs — build artifacts fetched by scripts/fetch-models.sh
public/models/*.tar.gz
# ...except the filler-tuned model, which is self-hosted (built once via
# scripts/lm-adapt) and shipped so the deployed site can serve it.
!public/models/vosk-model-small-en-us-0.15-fillers.tar.gz

# LM-adaptation corpus — regenerated by scripts/lm-adapt/generate-corpus.mjs
/text.txt

# LM-adaptation cached base model download
scripts/lm-adapt/.cache/

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?
